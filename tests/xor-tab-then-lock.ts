import * as anchor from "@coral-xyz/anchor";

describe("XOR Test 2 — settle-then-lock rejected via frontier guard", () => {
  it.skip("rejects lock_voucher whose cumulative_amount does not exceed max(spent, crystallized_cumulative) — assertion lands in Task 3 once lock_voucher exists", () => {
    // Stub: real test wired in Task 3.
  });
});
