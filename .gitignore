.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.deploy-backup/
.env
.env.*

# Local-only — not source (added 2026-06-07 cleanup)
.claude/
*.pdf
.deploy-resume.txt

# Local-only SSS security-scan baselines (analysis lives in private dexter-thesis)
.security-baseline/

# Fleet-migration resumable journal (audit trail; chain is truth)
scripts/.fleet-migration-journal.jsonl

# Leg-2 e2e proof artifacts (session/seller keys + run receipts stay local)
tests/.leg2-proof-keys.json
tests/.leg2-proof-receipts-*.json

# Fee-split canary run receipts (stay local)
tests/.canary-fee-split-receipts-*.json
